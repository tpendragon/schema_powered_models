require 'spec_helper'

describe SchemaPoweredModels do
  it 'has a version number' do
    expect(SchemaPoweredModels::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
